from random import *
from identificaLevel import identificaLevel

def drops(level=0):
    drops = identificaLevel(level)

    #for drop in drops:
     #  for item in drop:
      #    for i in item:
       #      for j in i:
        #        for k in j:
         #          for x in k:
          #              print(x, end='')
    return drops